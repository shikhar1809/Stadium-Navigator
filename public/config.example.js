// Stadium Navigator — runtime configuration EXAMPLE
// Copy this file to config.js (which is gitignored) and add your real key.
window.STADIUM_CONFIG = {
  GEMINI_API_KEY: "",  // paste your key here — this file is committed as a placeholder only
};
